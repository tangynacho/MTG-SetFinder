import MagicQuerier from "../util/MagicQuerier";
export interface Ruling {
    source: string;
    published_at: string;
    comment: string;
}
declare class Rulings extends MagicQuerier {
    byId(id: string): Promise<Ruling[]>;
    bySet(setCode: string, collectorNumber: string | number): Promise<Ruling[]>;
    byMultiverseId(id: number): Promise<Ruling[]>;
    byMtgoId(id: number): Promise<Ruling[]>;
    byArenaId(id: number): Promise<Ruling[]>;
}
declare const _default: Rulings;
export default _default;
